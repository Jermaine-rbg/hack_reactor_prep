// Variables for strings

// The variable is nameForMe and the value is Jermaine

// Jermaine is a string

//Basic variables are declared using the var or let keywords. You can assign variables and set variables using the = symbol.

let nameForMe = "jermaine"

console.log(nameForMe);

//Functions are the prime mechanism with which you will be manipulating data

// this function has one simple operation: it returns 1
function returnOne() {
  return 60;
}

// this part is similar to what happens when you click 'Run Tests'
var result = returnOne();
console.log(result); // should log 1 to the console

