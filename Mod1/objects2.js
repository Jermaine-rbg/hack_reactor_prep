// lets look at an object with boolean values
let booleanObj = {
    richRollin: true,
    organization: false
}

// object with number values
let numbersObj = {
    set: 60,
    street: 17
}

// object with string values 
let stringObj = {
    name: "rich rollin",
    currentTitle: "Crips",
    futurePosition: "OG"
}